BackEnd-Commands
* You can follow series of command to setup Express environment once you are in your project-name folder:

* npm install -> Will install all dependencies -> takes 10 to 15 min

* npm run start -> To compile and deploy the project in browser. You can press <Ctrl> key while clicking on localhost:8081 to open project in browser -> takes 2 to 3 min

* npm run jest -> to run all test cases and see the summary. It is mandatory to run this command before submission of workspace -> takes 5 to 6 min

* npm run test -> to run all test cases. It is mandatory to run this command before submission of workspace -> takes 5 to 6 min

* You need to use CTRL+Shift+B - command compulsorily on code IDE, before final submission as well. This will push or save the updated contents in the internal git/repository, and will be used to evaluate the code quality.


FrontEnd-Commands
* You can follow series of command to setup ReactJs environment once you are in your project-name folder:

* npm install -> Will install all dependencies -> takes 10 to 15 min

* npm run start -> To compile and deploy the project in browser. You can press <Ctrl> key while clicking on localhost:8082 to open project in browser -> takes 2 to 3 min

* npm run jest -> To run all test cases and check the summary.

* npm run test -> to run all test cases. It is mandatory to run this command before submission of workspace -> takes 5 to 6 min

* You need to use CTRL+Shift+B - command compulsorily on code IDE, before final submission as well. This will push or save the updated contents in the internal git/repository, and will be used to evaluate the code quality.
